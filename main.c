#include <stdio.h>

int main()
{
    int firstNumber = 30;
    int secondNumber = 40;
    int totalSum;

    totalSum = firstNumber + secondNumber;

    printf("Total Sum = %d", totalSum);

    return 0;
}
